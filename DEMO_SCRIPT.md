# DMS Agent 演示视频录制脚本

## 录制准备

### 环境检查

- [ ] 服务器正常启动：`python server.py`，确认日志显示 "DMS Agent server starting on http://127.0.0.1:8002"
- [ ] 浏览器打开 `http://127.0.0.1:8002`，等待左上角指示灯变绿（Agent Ready）
- [ ] 准备测试文件：
  - 源码文件若干（可从 `data/source_code/` 中选取 `process_video.py`、`state.py`、`extract_eye.py`）
  - 性能日志 CSV（需含 `timestamp`, `fps`, `latency_ms` 列，可自行准备或使用已有的测试数据）
  - 一份参考文档 PDF/TXT（作为知识库文档）

### 录制设置

- **录制工具**：OBS Studio（推荐）或 Windows Xbox Game Bar (`Win+G`)
- **分辨率**：建议 1920x1080，确保文字可读
- **音频**：录制系统音频（可选）+ 麦克风（可选解说）
- **浏览器**：清除缓存，隐身窗口，干净的起始状态
- **录制区域**：整个浏览器窗口（包含地址栏以显示 URL）

---

## 演示流程（总时长建议：8-12 分钟）

### 场景一：启动与首页预览（30 秒）

**画面内容**：从空白浏览器开始

1. 地址栏输入 `http://127.0.0.1:8002`，回车
2. 页面加载，展示三栏布局
3. 等待左上角状态指示灯从黄色 "Initializing" 变为绿色 "Ready"

**解说要点**：
> "DMS Agent 是一个面向驾驶员监测系统领域的 AI 数字工程师。启动后约 10-15 秒完成初始化。页面采用三栏布局：左侧导航、中间对话区、右侧功能面板。"

---

### 场景二：基础对话能力（1 分钟）

**画面内容**：展示不同类型对话

#### 2.1 知识问答（不调工具）

输入：`DMS 系统中 FPS 的合格标准是什么？`

展示：
- Agent 直接回答（基于内嵌领域知识，不调工具）
- FPS ≥30 优秀、≥15 合格、<15 不合格
- 回答专业、有数据、直接

#### 2.2 联网搜索（调 search_web）

输入：`RetinaFace 在边缘设备上有什么轻量化替代方案？`

展示：
- 工具调用组展开：`search_web` 运行中 → 完成
- DuckDuckGo 搜索结果
- Agent 综合分析结果并给出建议

**解说要点**：
> "Agent 能区分不同类型的请求。纯知识性问题直接回答，不需要调用工具。涉及最新技术动态时自动启动联网搜索。"

---

### 场景三：文件上传与代码分析（2 分钟）

#### 3.1 上传源码文件

1. 点击左侧导航栏 📁 上传按钮
2. 从 `data/source_code/` 选择 3-4 个 Python 文件上传
   - 建议：`process_video.py`、`state.py`、`extract_eye.py`
3. 右侧 "Files" 面板显示文件列表
4. 按文件夹组织（可选）：新建文件夹 → 拖拽文件进去

#### 3.2 代码结构分析

输入：`请分析我上传的源代码结构，告诉我主要有哪些模块和功能`

展示：
- 工具调用组展开（3 个工具）：
  - `scan_codebase` → 目录树和文件列表
  - `analyze_code_structure` → 类/函数/导入结构
  - `read_code_file` → 关键文件的具体实现
- Agent 综合输出项目结构总结

#### 3.3 代码细节查看

输入：`process_video.py 里的 detect_faces 函数具体是怎么实现的？`

展示：
- `read_code_file` 返回具体函数实现
- Agent 解释代码逻辑和 DMS 相关性

**解说要点**：
> "上传源码后，Agent 先用 scan_codebase 了解全貌，再用 analyze_code_structure 提取骨架，最后用 read_code_file 精读关键实现。这是专业代码审查的标准流程。"

---

### 场景四：性能日志分析（1.5 分钟）

#### 4.1 上传性能日志

1. 上传准备好的 DMS 性能日志 CSV 文件
2. 右侧面板显示文件

#### 4.2 日志分析

输入：`请分析我上传的性能日志，评估系统实时性`

展示：
- 工具调用：`parse_performance_logs` → 运行中 → 完成
- 输出 JSON 统计数据（样本数、FPS avg/max/min、延迟 avg/max/min、CPU、内存）
- **重点展示 DMS 判定**：
  - `[FAIL] FPS 不合格 (<15)，存在严重实时性问题`
  - `[!] 延迟偏高 (200-500ms)，需优化`
- Agent 基于数据给出具体的优化建议

**解说要点**：
> "性能日志分析是评估的核心步骤。Agent 解析 CSV 后自动计算各项统计指标，并按 DMS 行业标准进行判定。FPS 低于 15 判定为不合格，延迟超过 200ms 需要优化——这些都是基于 GB/T 国标的专业判断。"

---

### 场景五：国标知识库检索（1 分钟）

#### 5.1 国标检索

输入：`国标对疲劳检测告警延迟有什么具体要求？`

展示：
- 工具调用：`search_standards` → 返回 GB/T 国标条款
- 结果标注 `[GB/T National Standard]` 来源

#### 5.2 上传自定义知识文档

1. 在右侧面板 "Knowledge Base" 区域上传一份 PDF/TXT 参考文档
2. 文档出现在知识库列表中

#### 5.3 双轨检索演示

再次提问：`关于检测延迟，我上传的文档里有什么要求？`

展示：
- 搜索结果同时包含：
  - `[GB/T National Standard] 告警延迟 ≤ 行为持续时间的 50%...`
  - `[User Document - xxx.pdf - Clause 2] 本项目要求延迟低于 200ms...`
- Agent 对比两个来源给出综合判断

**解说要点**：
> "知识库双轨检索是这个系统的一大特色。search_standards 同时搜索全局 GB/T 国标库和用户上传的项目文档，合并展示。这让 Agent 能够同时参考国家标准和项目具体规范。"

---

### 场景六：代码修改与 Diff 对比（1.5 分钟）

#### 6.1 提出修改需求

输入：`state.py 中疲劳判定的 EAR 阈值目前是 0.2，请帮我改成 0.18，并解释为什么`

展示：
- Agent 先用 `read_code_file` 确认当前代码
- 解释修改原因：降低阈值 = 更早检测到疲劳苗头 = 提高灵敏度
- 调用 `modify_code` 执行修改
- **重点展示 Diff 卡片**：
  - 左侧：原代码（红色 - 行）
  - 右侧：新代码（绿色 + 行）
  - 风险评估：`[OK] 低风险`
- 右侧 "Modified Files" 面板显示修改记录

#### 6.2 下载修改后的文件

- 点击 Diff 卡片上的下载按钮
- 或从右侧 "Modified Files" 面板下载

**解说要点**：
> "代码修改遵循严格的流程：先阅读确认、再解释原因和风险、最后执行修改。修改后生成 unified diff 对比，用户可以清晰看到每一行变化。系统会自动检测涉及安全关键词的修改并标注高风险。"

---

### 场景七：指标对比（1 分钟）

输入：`我做了优化，请帮我对比优化前后的性能变化。优化前是 perf_log_v1.csv，优化后是 perf_log_v2.csv`

展示：
- 工具调用：`compare_metrics` → 运行中 → 完成
- **Markdown 表格**：FPS、延迟、CPU、内存的变化量和百分比
- 判定标签：`[OK] 显著改善` / `-> 基本持平` / `[!] 恶化`

**解说要点**：
> "compare_metrics 工具自动计算四项核心指标的前后变化，并按变化幅度给出判定——变化小于 5% 为基本持平，超过 30% 为显著改善。"

---

### 场景八：报告生成（1 分钟）

#### 8.1 触发报告生成

1. 点击右侧面板底部 "Generate Report" 按钮
2. 或输入 "请对我的 DMS 系统做一次完整的性能评估"

展示：
- Agent 自动执行完整评估流程：
  - `search_standards` 获取国标依据
  - `parse_performance_logs` 分析性能数据
  - `analyze_code_structure` 检查代码结构
  - `save_report` 保存报告
- 多个工具调用依次展开/折叠

#### 8.2 报告成果

- 右侧 "Reports" 折叠面板自动展开
- 显示新生成的报告文件名和生成时间
- 点击文件名可直接下载 Markdown 报告

**解说要点**：
> "点击生成报告后，Agent 自动执行完整的评估流程。报告保存在 reports 目录，同时列出在右侧面板中。用户可以随时下载查看。报告为 Markdown 格式，包含数据分析、标准对照和优化建议。"

---

### 场景九：多会话与数据持久化（1 分钟）

#### 9.1 创建新会话

1. 点击右侧 "Sessions" 区域的 "+" 按钮
2. 创建第二个会话
3. 切换到新会话，展示空白状态

#### 9.2 上传不同文件

1. 在新会话中上传不同的测试文件
2. 切换回第一个会话——文件、对话、报告都还在
3. 展示各会话独立的文件列表和对话历史

#### 9.3 页面刷新恢复

1. 按 F5 刷新页面
2. 对话历史、文件列表、报告列表、知识库文档全部自动恢复

**解说要点**：
> "项目支持多会话管理，每个会话拥有独立的文件、对话历史和知识库。所有数据通过 localStorage 持久化，页面刷新后自动恢复。同一个 local_session_id 的会话在服务器重启后也能继续使用已上传的文件。"

---

### 场景十：附加功能展示（1 分钟）

#### 10.1 导出对话

1. 点击对话区右上角导出按钮
2. 选择 "Export as Markdown"
3. 下载的 Markdown 文件展示

#### 10.2 中英文切换

1. 点击语言切换按钮
2. 界面切换到英文
3. 再切回中文

#### 10.3 ZIP 批量下载

1. 点击右侧面板 "Download All" 按钮
2. 下载包含所有上传文件的 ZIP 包

**解说要点**：
> "除了核心的 AI 评估功能，系统还提供了对话导出、中英文界面切换、ZIP 批量下载等实用功能。"

---

### 结尾（15 秒）

展示项目 GitHub 地址（如有）或总结画面。

**解说要点**：
> "DMS Agent — 为 DMS 系统工程师打造的 AI 数字评估助手。基于 LangGraph 多轮工具调用、FAISS 向量知识库、DeepSeek 大模型，覆盖从代码分析到性能评估到合规检查的完整工作流。"

---

## 快速录制清单（简化版）

如需快速录制（3-5 分钟），按以下顺序展示核心功能：

| 序号 | 场景 | 时间 | 核心展示点 |
|:--:|------|:--:|-----------|
| 1 | 上传源码 + 结构分析 | 40s | scan_codebase → analyze_code_structure |
| 2 | 性能日志解析 | 30s | parse_performance_logs + DMS 判定 |
| 3 | 国标检索 + 知识库双轨 | 30s | search_standards 合并展示 |
| 4 | 代码修改 + Diff | 40s | modify_code + diff 卡片 |
| 5 | 指标对比 | 20s | compare_metrics 表格 |
| 6 | 生成报告 | 40s | 完整评估流程 + 报告下载 |
| 7 | 多会话/持久化 | 30s | 切换会话 + 刷新恢复 |

---

## 录制后处理建议

- 使用 OBS 录制为 MP4 (H.264)
- 可添加背景音乐（低音量，不干扰解说）
- 关键操作处可添加放大效果（如 OBS 的 "放大" 滤镜）
- 工具调用展开/折叠处可暂停 1-2 秒让观众看清内容
